# ---------------------------------------------------------------------
# 1. SEGURANÇA: NUNCA subir senhas, credenciais ou chaves
# ---------------------------------------------------------------------
.env
html/app/etc/env.php
.my.cnf
*.key
*.pem


# ---------------------------------------------------------------------
# 2. DADOS PESADOS: NUNCA subir volumes do Docker ou ISOs/Mídias
# ---------------------------------------------------------------------
data/
!data/.gitkeep
html/pub/media/*
!html/pub/media/.htaccess

# ---------------------------------------------------------------------
# 3. MAGENTO: Ignorar caches, dependências e arquivos gerados
# ---------------------------------------------------------------------
html/vendor/
html/var/*
html/generated/*
html/pub/static/*

# ---------------------------------------------------------------------
# 4. Relatórios de varredura
# ---------------------------------------------------------------------
html/security_reports/

# ---------------------------------------------------------------------
# 5. Chaves e certificados reais
# ---------------------------------------------------------------------
nginx/ssl/*.key
nginx/ssl/*.pem

# ---------------------------------------------------------------------
# 6. Mantém os exemplos (DEVEM vir depois das regras de ignore)
# ---------------------------------------------------------------------
!nginx/ssl/*.key.example
!nginx/ssl/*.pem.example
!nginx/ssl/.gitkeep

# ---------------------------------------------------------------------
# 7. IDE / Editor
# ---------------------------------------------------------------------
.idea/
.vscode/
*.swp
*.swo
*~

# ---------------------------------------------------------------------
# 8. Sistema Operacional
# ---------------------------------------------------------------------
.DS_Store
Thumbs.db

# ---------------------------------------------------------------------
# 9. Docker (overrides locais)
# ---------------------------------------------------------------------
docker-compose.override.yml

# ---------------------------------------------------------------------
# 10. Logs
# ---------------------------------------------------------------------
*.log
html/var/log/*

# ---------------------------------------------------------------------
# 11. Backups
# ---------------------------------------------------------------------
*.sql
*.sql.gz
*.tar.gz
*.zip
backup-*.sh

# ---------------------------------------------------------------------
# 12. Node (caso você utilize)
# ---------------------------------------------------------------------
html/node_modules/
html/package-lock.json

# ---------------------------------------------------------------------
# 13. Composer lock (opcional)
# ---------------------------------------------------------------------
composer.lock
html/composer.lock
